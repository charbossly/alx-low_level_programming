#include "main.h"

/**
* is_prime_number - This is a description
* @n: Description of parameter n
* Return: integer
*/

int is_prime_number(int n)
{
	if (n <= 1)
	{
		return (0);
	}
	if (
	{
		return (n * factorial(n - 1));
	}
}
